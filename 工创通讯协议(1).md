# 上位机极简通信协议

## 串口参数

- 接口：UART7
- 波特率：115200
- 数据格式：8N1
- 数据方向：上位机发送，STM32接收
- 不使用ACK；需要时由上位机重复发送

## 帧格式

```text
AA 55 CMD LEN PAYLOAD... CHECKSUM
```

| 字段 | 长度 | 说明 |
|---|---:|---|
| `AA 55` | 2字节 | 固定帧头 |
| `CMD` | 1字节 | 命令 |
| `LEN` | 1字节 | PAYLOAD字节数，最大4 |
| `PAYLOAD` | 0～4字节 | 命令数据 |
| `CHECKSUM` | 1字节 | `(CMD + LEN + PAYLOAD所有字节) & 0xFF` |

颜色编号统一为：

```text
0 = 未识别
1 = 红色
2 = 绿色
3 = 蓝色
```

## 命令

### 0x01 设置物料顺序

```text
LEN = 3
PAYLOAD = COLOR_1 COLOR_2 COLOR_3
```

三个颜色必须是 `1、2、3` 的不重复排列。状态机到达二维码位置后，只有收到合法顺序才会继续。

示例：顺序为绿、蓝、红（2、3、1）：

```text
AA 55 01 03 02 03 01 0A
```

### 0x02 视觉反馈

```text
LEN = 4
PAYLOAD = COLOR OFFSET_X OFFSET_Y IS_STATIC
```

| 字段 | 类型 | 说明 |
|---|---|---|
| `COLOR` | `uint8` | `0`=未识别，`1`=红，`2`=绿，`3`=蓝 |
| `OFFSET_X` | `int8` | X 偏差，单位毫米，范围 -128～127 |
| `OFFSET_Y` | `int8` | Y 偏差，单位毫米，范围 -128～127 |
| `IS_STATIC` | `uint8` | `0`=物体运动中，`1`=物体静止 |

STM32始终保存合法颜色。在 `STATE_ADJUST_*` 状态中：

- `IS_STATIC=0`：按照 `OFFSET_X/OFFSET_Y` 执行一次微调。
- `IS_STATIC=1`：不执行偏差移动，产生 `EVENT_ADJUST_DONE` 并进入下一动作。

示例：识别为绿色，X=-10 mm，Y=5 mm，物体仍在运动：

```text
AA 55 02 04 02 F6 05 00 03
```

## 异常处理

- 帧头、长度、校验或命令参数非法时直接丢弃。
- 解析器支持半帧、连续多帧和帧前噪声，并自动重新搜索 `AA 55`。
- 物料任务开始后，本轮物料顺序不再允许修改。
- 非视觉调整状态收到视觉反馈时只保存合法颜色，不执行偏差移动或状态切换。
